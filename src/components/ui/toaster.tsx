'use client'

import * as React from "react"

interface ToasterProps {
  className?: string
}

export function Toaster({ className }: ToasterProps) {
  return null // Placeholder para o componente de toast
}